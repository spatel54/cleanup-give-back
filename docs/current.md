# Current state

What runs in the repo today.

## Runnable today

- **Admin portal (`admin/`)** — **ARCHIVED 2026-07-30.** Do not run or deploy. Successor is **`admin-web-app/`**. Keep using [`admin/db/*.sql`](../admin/db/) for shared Supabase migrations. Details: [admin/README.md](../admin/README.md), [admin/ARCHIVED.md](../admin/ARCHIVED.md). Historical feature notes remain under [`docs/admin/`](admin/).
- **Web app** in `admin-web-app/` — Next.js admin UI on **http://localhost:3000** (`npm run dev` in `admin-web-app/`); production on **https://admin.example.com** (Vercel project `example-admin-web`, deployed 2026-08-17). **`/login`** (branded cream gradient-bars background + email/password) is live; `src/middleware.ts` enforces admin claim when `BYPASS_AUTH` is off (`/login` still previewable while bypass is on). Shell: hamburger + overlay below `lg` (1024px), hover icon rail at desktop; tablet layouts stack filter toolbars and use 2-col KPI/chart grids. **Live-wired to the same Supabase project as the mobile `frontend/`** — Dashboard/Sessions/Feedback/Orders/Payments/Users/Volunteers/Events/Insights/Analytics read real `sessions`, `volunteer_feedback`, `shop_orders`, `donations`, `court_orders`, `events`, and Auth-user rows. **Home metric tiles** (Waiting / Approved / Hours / Feedback) show a signed period delta + “vs prior”; Approved/Hours/Feedback follow `PeriodToggle`, Waiting stays live open-queue. Home **Hours & submissions** fills the same row height as **How long sessions wait**; the plot stays `h-52` and is vertically centered so it does not stretch. **PeriodToggle** presets are Today / Month / Year / All / Custom (30-day removed). Title + that bar stay sticky (cream fill, no content showing through) on Home, Sessions, Users, Insights, Feedback, Shop Orders, Payments, and Audit Log. Sessions also pins search, status chips, Court-ordered only, Compare, and the session count. Audit Log also pins search, All actions, and Search. **`/sessions` defaults to All time** (redirect when no `period`/`from`/`to` in URL) so the approve queue is visible on first open; Dashboard Waiting/Approved/Hours tiles link to `/sessions?period=all`. Home **Needs you** lists the same recent submitted sessions as `/sessions` (newest `created_at` first), clips to **20**, and **View more** opens `/sessions?period=all`; the heading count stays open `under_review`. **Sessions, Orders, and Feedback** lists (and their KPIs) follow PeriodToggle via `filterByListPeriod` — **Month = rolling last 30 days**. Home / Insights keep calendar month. **Payments** Month = last 6 months (monthly bars), Year = last 6 years (yearly bars). **Feedback** (`/feedback`) has PeriodToggle plus client-side rating filters (All / Excited → Very Sad) and clickable distribution columns. **Admin lists never fall back to fixtures** (empty → empty real lists, including orders/feedback/payments/revenue/events/insights). Volunteer profile (`/volunteers/[id]`) KPI strip includes **Miles Walked** (sum of approved session `distance_miles`) alongside Sessions and Approved Hours. `/events/new` and `/events/[id]/edit` write real events + photos; event detail supports publish/unpublish/delete (the earlier at-risk-volunteer event-notify widget was removed 2026-08-11 along with the rest of the due-date/hours-required "at risk" model); `/sessions` supports inline Approve/Decline moderation (writes `sessions` + `admin_audit_log`, emails/pushes the volunteer and inserts `volunteer_notifications` before push) plus **bulk approve** via row checkboxes, and renders live `active` (in-progress) rows without crashing; the session preview drawer adds **Hours Adjustment** (also writes a volunteer inbox row + push), **Admin Notes** (silent), **Letterhead PDF** generation (proxies to `backend/sessions` via `SESSIONS_API_URL`/`ADMIN_API_KEY`), and **live Walking Path + Photos** when available. Session red-flag badge hover explains each flag (This session vs This volunteer); click opens a modal. Invalid (30d) and deleted (all-time) are separate flags, and a zero count is omitted (MapLibre polyline from `sessions.route` with Play/Pause/Replay + **fullscreen**, Start/End labels, checkpoint **photo thumbnails along the trail** — prefer stored `checkpoints.latitude`/`longitude` when present, else time-along-route — and signed grid photos from the `session-photos` bucket via `loadSessionEvidence`). Session list/drawer tags: `CourtBadge` for `sessions.court_ordered`; `ServiceTypeBadge` for onboarding `service_type`, omitted when that type is **Court Ordered** on a court-ordered session. Session red-flag badge: hover card explains each flag; click opens a modal grouped This session vs This volunteer (invalid and deleted are separate flags; a zero count is omitted). Checkpoint GPS columns: run [`admin/db/007_checkpoint_coordinates.sql`](../admin/db/007_checkpoint_coordinates.sql) on Supabase; mobile + Fly API embed coords on new checkpoint submits. Copy `admin-web-app/.env.local.example` → `.env.local` with the shared Supabase project (plus `RESEND_API_KEY`/`EMAIL_FROM`/`ADMIN_NOTIFY_EMAIL` for mail, optional `SESSIONS_API_URL`/`ADMIN_API_KEY`) to go live. **Resend is live** for local admin + Fly (domain `cleanupgiveback.org` verified 2026-08-03; smoke-tested). Vercel production still needs the same Resend env vars for hosted admin email. Donations revenue reads `public.donations` (`admin/db/006_donations.sql`); empty windows stay at $0. **Payments → Revenue share** shows MoM shop % + per-item share-point deltas + top mover. **Realtime session refresh (2026-08-04, hardened 2026-09-14):** the admin shell (`SidebarDemo`) subscribes to Supabase Realtime on `public.sessions` (`useSessionsRealtimeRefresh`) and also polls every 15s / on tab focus so Home "N to review" updates when mobile finalizes. Requires `admin/db/008_admin_sessions_realtime_read.sql` (RLS + publication) and `admin/db/030_sessions_replica_identity_full.sql` (FULL replica identity for UPDATE events). Service-role session reads use `cache: 'no-store'`. **Decision templates, volunteer timeline, readiness page, court packet (2026-08-07):** the session drawer's Decline action now offers hardcoded reason templates + admin-note snippets (`lib/decisionTemplates.ts`); `/volunteers/[id]` gained a chronological Timeline section sourced from `admin_audit_log` (including a new `'email sent'` audit action); `/settings` gained a Production Readiness panel probing Resend/Sessions API/Supabase/storage/Realtime; the Letterhead section gained an "Export Court Packet" link (court-ordered sessions) that adds a cover sheet + adjusted-hours annotations to the existing service-letter PDF. **Attention inbox, communication log, session compare, editable email templates (2026-08-07):** new **`/attention`** page aggregates sessions-under-review, flagged feedback, order issues, failed emails, red-flagged sessions, and data-quality alerts (`lib/attention-inbox.ts`, `lib/data-quality.ts`) into one queue; `/volunteers/[id]` gained a **Communication** section (email send log via new `admin/db/010_email_log.sql` `email_log` table, wired into every send site, plus manual contact notes) with an optional Resend delivery-status webhook at `/api/webhooks/resend` (inert until configured in the Resend dashboard); `/sessions` gained a **Compare sessions** mode → `/sessions/compare` for same-volunteer side-by-side photos/route/notes; the Audit Log's before/after diff is now stacked from→to pills in `AuditDiffCard.tsx` (per-value tint, not a Before/After column grid); and **shipped-order email is now sent** (previously no send existed) on the pending/paid → shipped transition. **`/emails` (2026-08-09):** Compose shows read-only **From** (`EMAIL_FROM`), unified To, optional Cc/Bcc, font/size/color, Send or Schedule; schedule send uses a custom date + 15-min time-dropdown picker instead of the browser's `datetime-local` UI; Send/Schedule buttons show an inline hint listing what's missing (recipient/subject/message) when disabled; **Scheduled** tab edit/cancel/send-now (`admin/db/013_scheduled_emails.sql` + cron); Templates use everyday insert chips (no mustache in UI). **Court hours + emails + map rework (2026-08-11):** the **`/court-risk`** dashboard and every "hours remaining" display (volunteer profile, Users list, session-approve overshoot gate, Insights chart, event at-risk-notify widget) were removed — completed hours are derived live and reset to zero on service-letter generation (`court_orders.hours_reset_at`), with a manual + bulk **Reset hours** action for Admin (`CourtOrderForm`, `/sessions` bulk-select); the Emails tab now lists just 2 editable templates (**Hours reminder** — new daily cron `/api/cron/send-hours-reminders` nudging court-ordered volunteers idle 7-10 days — and **Order tracking**, renamed from "Order shipped"), while `approved`/`declined`/`event_registration` keep sending automatically; the Emails tab's "To"/Cc/Bcc pickers now list every volunteer (sorted, no 8-row cap), auto-fill tracking number/carrier for the Order tracking template from the volunteer's real latest `shop_orders` row, and a volunteer profile's "Send email" button deep-links to `/emails?to=<id>` with the recipient prefilled. The US activity map (`UsHeatmap.tsx`) dropped only its neighborhood/tract drill tier (Census tracts + Nominatim reverse geocoding removed) — nation→state→county stays exactly as before. See [admin-web-app.md](admin-web-app.md).
- **Expo app** in `frontend/` — Expo SDK 57, React Native 0.86, Expo Router
- **HTML prototype mode** — `frontend/src/app/prototype/[screen].tsx` renders Stitch HTML screens in a WebView with JS navigation bridge
- **Entry flow** — Splash → `/welcome` (**Log In** email/password + horizontal Apple/Google/Facebook under OR; tertiary underlined **Sign up** → **`/create-account`**). Home requires a registered session; incomplete onboarding resumes at `/account-phone` (missing phone/birthday/service type), `/how-it-works` (unpaid), or `/device-permissions` (paid). Full signup path: create-account (**email draft only** — no `signUp` yet) or social OAuth first → account-phone (name/email prefilled for social) → account-details (under-13 → `/under-age`; social deletes/signs out if already authenticated) → age ≥ 13 creates or syncs auth → `/creating-account` → **`/how-it-works`** (Pay $59.99 or company code; redeem needs JWT) → **`/device-permissions`** (Location / Camera 18+ / Notifications stacked Allow rows) → `/setup-complete` → Home. `/camera-permission`, `/location-permission`, and `/notification-preference` remain in the repo but redirect to `/device-permissions`. Free-hour / free-kit / tours remain in the repo but are **skipped**. Track requires payment (no free orientation hour on the active path). Account: **Letters** → `/letters` (library of PDFs already downloaded; search/sort; empty **No letters downloaded**; row → `/letter-detail`; floating **Export** FAB → **Export all** / select + combined-PDF **Share** / **Cancel**); Records **Download Service Record** is a second link to the same date-range export as Sessions; **FAQ** accordion then **Give Feedback**; Membership block hidden; approved-only export; profile photo; Preferences **Notification Preferences**; role pill; **Download my data** on delete confirm; footer © nonprofit (no version string). **Notifications** Messages: swipe right → pin (unpinned, amber) or unpin (pinned, gray `#6e7a6c`); swipe left → delete; **Clear all** uses an iOS confirm alert; pinned rows under a **Pinned** header with a pushpin left of the date. Upcoming Events on Home are hidden.
- **Session Tracking** — Production routes under `frontend/src/app/` import `liveSessionStore` + map/checkpoint components from `frontend/src/features/session-tracking/` (GPS, `expo-camera`, Fly API, WebView maps in Expo Go / native MapLibre in EAS). **Pre-session start photos Cancel** returns to `/session-setup` with the filled form (not Home). **iOS Lock Screen (2026-09-03, restyled 2026-09-10, layout fix 2026-09-13):** ActivityKit Live Activity shipped in **TestFlight build 51** — Lock Screen card is **two-tone premium**: **Live** = primary green `#009540` top bar flush to soft `green/50` `#f7fff1` body (no white seam; `VStack(spacing: 0)` + `.contentMarginsDisabled()` + bar-colored outer fill); **Photo due** = declined red `#BA1A1A` top bar + soft `red/50` `#ffd9de` body. **Photos on:** **mi | TIME | next photo** with Strava-style center hero (~40pt IBM Plex SemiBold vs ~20pt sides + **time** label); hero is a centered overlay on a `0:00` stub so `Text(timerInterval:)` cannot shift or shrink the clock; side labels sit near the card edges. **Photos off:** **mi | TIME**. **Photo due:** **mi | TIME | remaining** (10-minute grace countdown on the right). Checkpoint progress bar tints **green → amber → red** as the 30-minute window elapses (`<70%` green, `70–90%` amber `#f0ad1e`, `≥90%`/overdue red). **Dynamic Island compact:** logo leading + timer trailing (fixed width; not left-packed). Fonts: Noto Sans SemiBold + IBM Plex Sans SemiBold in the widget extension. Elapsed + checkpoint timers use SwiftUI `timerInterval` (OS-driven, not per-second JS). Bridge passes `checkpointWindowStartedAt`, `photosEnabled`, `checkpointDueOrGrace`. No interactive buttons in the widget (only whole-card tap-to-open) — the "Allow Live Activities from…" sheet is iOS system UI and cannot be restyled. **Requires a new EAS iOS build** to pick up `SessionLiveActivity.swift` (widget extension is not Metro-hot-reloaded). `@kingstinct/react-native-activity-kit` + `@bacons/apple-targets`, App Group `group.com.example.cleanupgiveback`, widget pod via `withLiveSessionWidgetPods.js`, iOS deployment **26.0**. JS bridge no-ops in Expo Go. Checkpoint submit resolves WGS84 via `resolveCheckpointCaptureCoords` and posts `latitude`/`longitude` to Fly so admin Walking Path pins use real capture locations. **Live tracker navbar (2026-08-18):** location/weather pill is viewport-centered in the top row (188px; equal 10px inset on each side of the divider; long place names ellipsize). **Pre-session guide (2026-08-12 / 2026-09-01):** Skip and free-kit Continue resolve the next real screen (location if needed, then Allow Camera for 18+, else finale) in one forward transition. 18+ always see Allow Camera after location (10-pill bar: location=8, camera=9, complete=10; totals do not shrink when a permission was already granted). Under-18 still skips camera. **Live timer (2026-09-10):** static card — no expand/collapse, no flap/notch chevron; always shows elapsed time + Distance (continuous pulsing lime border, plain rounded-rect). Checkpoint photos keeps its own independent header chevron. **Photo Retake** on `/photo-capture` cross-fades the BeReal preview into the camera on a black host (instant under reduced motion). Checkpoint zoom **1×** is the wide / full-sensor frame; **0.5×** uses the iOS ultra-wide when available, otherwise matches 1× (no digital crop on single-lens cameras). **Night flash warning:** on the back-camera step, if GPS+date is nighttime and flash is off, `FlashWarningModal` shows once per capture (Turn on flash / Continue without); fails open if location is unavailable. **Branded loading (2026-08-18):** data-fetch screens use `BrandLoadingView` (broom-sweep animation + Sanchez label); session finalize / free-trial Pay Later use the same overlay (**Saving session…**, 3.5s minimum). **Free-hour paywall (unpaid):** orientation clock still tracked internally (`FREE_TRIAL_DURATION_SECONDS`, `__DEV__` override `EXPO_PUBLIC_FREE_TRIAL_SECONDS`) but no longer shown on the live timer card (removed 2026-09-10); no Skip orientation; hour expiry does not open a modal; **End Session** logs an approved 1-hour free hour, opens session details, and consumes orientation; the next unpaid Track shows `/tracker-paywall` (**Pay now** / **Pay Later**, no session). Spec: [free-hour-tracker-paywall.md](frontend/specs/free-hour-tracker-paywall.md). **Checkpoint camera zoom (2026-09-02, framing 2026-09-11):** with an optical ultra-wide, 0.5×→1× is a real wide-lens hand-off; without one (every front camera, single-lens backs) 0.5× and 1× are both the sensor's full frame — no digital 2× crop — so 1× no longer feels over-zoomed on the selfie. Above 1× still zooms in. `frontend/patches/expo-camera+57.0.4.patch` (regenerated 2026-09-10 for the SDK 57 bump) adds an absolute `zoomFactor` prop and reports the device's real zoom range ([ADR-007](adr/ADR-007-patched-expo-camera-zoom.md)). **Requires a native rebuild** (`npx expo prebuild` / pod install, or a new EAS build) for the patched path — Expo Go uses the approximate 0–1 fallback with the same no-crop rule. The folder also keeps a **legacy mock** `dev/PreviewApp.tsx` harness for isolated UI review (see the feature's [README.md](../frontend/src/features/session-tracking/README.md)).
- **Home dashboard (`frontend/src/features/figma-screens/screens/HomeScreen.tsx`)** — mounted at `/` with Figma-exact SVG icons, interactive week picker (arrow weeks + calendar; short month names in a date badge that stretches to **Week N** / **This week**; calendar modal uses centered **36×36** day circles with column gaps so week-highlight and selected days do not touch; when off the current week, a quiet **This week** chip restores it), and shared `BottomNavBar`. Service Hours chart, total hours, weekly-hours lime pill (`textPrimary` on `accentLime`, “N hours this week”), and **Your Impact** (`ImpactFeedSection`: two-line month/year sentence, list-only full-width picker sheet — years/months with session activity plus the current year/month, Reanimated + scrollable `FlatList`; Recent Cleanups map-first tiles — static CARTO Voyager snapshot from persisted `mapAnchor` (neutral surface when no GPS), mini photo + status from `impactFeedStore` + `sessionStatsStore`). Sub-hour weeks show minute-scale bars; each filled bar shows an integer (hours, or minutes) with no units; Y-axis ticks keep inline units (`15 min`, `2 hrs`, etc.). Home does not show a Recent Sessions list. First-time empty uses shared `EmptyState` (Service Hours “No service hours yet” + Track-button copy + **Log session?**; Impact the same CTA). Recent Cleanups **View All** opens `/sessions-list`. **Upcoming Events** load published rows from Supabase `public.events` on focus (`eventsApi.ts`); heading aligns with Your Impact; empty catalog stays visible with `EmptyState` (no mock-event fallback); up to three cards (tap opens `/event-detail`). Returning-user populated copy: `HomeScreenReturningUser` + `mocks/home.returningUser.ts` (PreviewApp). **Sessions** tab opens `/sessions-list` (Figma `515:1791`); **Profile** tab opens `/account`. When a live session is active (`liveSessionStore.isActive`), a minimized tracker pill (`LiveSessionMinimizedPill` via shared `LiveSessionBottomNavStack`) shows on **every** bottom-nav screen — the pill floats above the nav bar without a white fill behind it (Home-style chrome). Live session back chevron (down) minimizes via immediate `dismissTo('/')` (session keeps running; pill stays on non-tracker screens). Shop Track resumes the live session when active (does not reopen setup). **Court hours (2026-08-11):** the Court Progress card and `GET /me/court-progress` endpoint were removed — the app no longer tracks or displays "hours remaining to complete." Completed hours are derived live from approved court-ordered sessions and reset to zero every time a service letter/court packet PDF is generated (`court_orders.hours_reset_at`); the Account screen shows a static disclaimer to that effect for volunteers with `user_metadata.service_type === 'Court Ordered'`.
- **Event detail (`/event-detail`)** — native `EventDetailScreen` (Figma `events_detail`, `196:226`): loads published event by `?id=` from Supabase only; unknown/unpublished/missing id shows `EmptyState` **Event not found** + **Go Home**; hero image carousel from `events.image_urls` (falls back to `image_url` / app default), badges, overview, organizer (logo + Instagram/Facebook/YouTube icons), what to bring (clothing/water/shoes glyphs); copy icon copies Maps link with toast; location section shows a live MapLibre pin preview (WebView in Expo Go, native in dev-client; Maps CTA on web) and opens Apple Maps (iOS) / Google Maps (Android) on tap; **Add to calendar** (calendar glyph + label) via `expo-calendar`; **Register** CTA uses Noto Sans SemiBold and shows `EventRegistrationSuccessModal` (Figma `787:406`); **Go Home** returns to `/`. Event detail icons are `react-native-svg` ports (not `expo-image` SVG requires).
- **Shop (`/shop`)** — native `ShopScreen` (Figma `498:606`): donate card with thank-you bags illustration on top (152px, slight primary linear wash, 0%→36% opacity top→bottom, no heart watermark) and copy/amounts below (no donate icon); featured Trash Clean Up Kit with **Best Seller** badge (12px + flame), **Noto Sans Bold 20px** title and **$49.99** on one row (**In stock** below); product grid (tote, grabber, vests); **Add to cart** → empty-by-default `cartStore`; cart badge = live qty; empty cart icon tap shows toast; cart icon → `/cart` when items exist.
- **Donate (`/donate`)** — native `DonateScreen` (Figma `412:4` / PRD §6.20): Contribute top bar, impact card, amount presets + custom; sticky Continue footer floats above the keyboard with a white filler (no hollow gap); Shop → Custom scrolls the custom amount field into view above footer+keyboard; Cart **Custom** opens Contribute (`from=cart`) and back returns to Cart; **Continue** → Stripe Checkout → `/purchase-confirmation?mode=donation&amount=` on success.
- **Product detail (`/product-detail`)** — native `ProductDetailScreen`: cleanup-kit **Best Seller** badge (12px + 16px flame); qty + **Add to cart** → `cartStore`; tap a carousel image → fullscreen **Product images** viewer (swipe or tap bottom thumbnails, top-right close); empty-cart toast on cart icon; cart badge live; tote-bags / cleanup-kit / adult + child vest carousels lead with upright product flatlay photos (prior slides retained); tote Earth / Ocean / Earth-and-Ocean split last (angled slash through the circle); split selected by default shows group + all variants; Earth/Ocean filter when a solid swatch is chosen.
- **Cart (`/cart`)** — native `CartScreen`: line items (name + product description preview, 2 lines truncated with ellipsis) + donation from shared `cartStore` (default **$5**); **Custom** donation opens Contribute; descriptions come from product detail when adding from shop/product detail; **Continue** → `/checkout`.
- **Checkout (`/checkout`)** — native `CheckoutScreen`: order summary from `cartStore`; shop **Place Order** → Stripe. Tracker mode is **$59.99** mock (card fields + `markTrackerPaid`; IAP deferred) with kit on by default; **Remove** on kit line or **Add free cleanup kit** card to opt back in (same total). Shop USPS shipping is **25% of paid product-item subtotal**.
- **Purchase confirmation (`/purchase-confirmation`)** — loads live order/donation rows by id; shows Stripe payment method (or “Processing…” until webhook); hearts animate on enter.
- **Sessions list (`/sessions-list`)** — native `SessionsScreen` (Figma `sessions_list___hybrid_redesign`, `515:1791`): search bar; horizontally scrollable All / Approved / Under Review / Declined filter chips (content-sized — **Under Review** no longer clips); selected chip uses primary green fill + white label; fixed bottom-right **Export record** FAB (primary pill above the tab bar; enters multi-select → **Export all** with no-approved alert else date-range PDF/CSV, **Share** OS sheet, **Cancel**; hidden in Select / export mode); sort dropdown with **Select** on the same row; refetches on focus from Fly API when `EXPO_PUBLIC_API_URL` is set; initial fetch shows inline spinner; empty list (new users / no logged sessions / API unset) shows **No sessions logged yet.** with **Log session?** → `/session-setup-guide`; **Select** mode for bulk hard delete of non-approved sessions only; row tap opens `/session-detail?id=` (when not selecting).
- **Shop home (`/shop`)** — native `ShopScreen` (Figma `shop_home`, `498:606` / PRD §6.19): mission copy, donate card (thank-you bags illustration header, copy and $5/$10/$15/Custom below), featured Trash Clean Up Kit (**Best Seller** badge, bold 20px title + inline **$49.99**), 2-column product grid (Tote $3 / Grabber $23.99 / Child Vest $9.99 / Adult Vest $12.99); Shop tab active; Home/Account/Sessions nav wired to `/shop`.
- **Session detail (`/session-detail`)** — native `SessionDetailScreen` (Figma `session_detail`, `515:1848`): `useSessionDetail` fetches from Fly API + signed Supabase Storage photo URLs; initial fetch shows inline spinner; missing/failed load shows `EmptyState` + **View sessions**; when Admin declined with a reason, a **Reason not approved** card shows the volunteer-facing `decline_reason` (private admin notes are never exposed); walking-path map via `SessionRouteMapPanel` + `useSessionRouteCoordinates` with icon **Play / Pause / Replay** controls and synced replay timer; auto-replays once on load (skipped when reduced motion is enabled) on the basemap layer selected when the session ended (`CompletedSessionSnapshot.mapLayer`); pan/zoom when route exists; status badge, title/date/address, hours/miles/photos stats, shared `SessionPhotosSection` **Photos** carousel matching post-session confirmation (horizontal selfie/progress thumbs with time pills; tap → full-screen enlarge); read-only **Description** (setup text) and editable **Notes** (500-char limit with live counter, persisted locally via `sessionNotesStore` — editable when revisiting from Sessions list). Sticky footer: outlined **Share Feedback** → `/session-feedback`; **Delete session** (non-approved only) calls `DELETE /sessions/:id` when synced and clears local recent/cache/stats + AsyncStorage tombstones; **Download PDF** when approved (Fly service letter + `expo-sharing`); filled tertiary **Go Home** (no stroke). After delete, returns to `/sessions-list` with row removed. Back chevron → Home. Spec: [free-hour-tracker-paywall.md](frontend/specs/free-hour-tracker-paywall.md) (Pay Later entry + Go Home ACs).
## Design ground truth

- **Figma** is now the authoritative design source — [CleanUpGiveBack file](https://www.figma.com/design/DrDcQH14n7ntDQ80F7au9S/CleanUpGiveBack?node-id=1-3) holds 7 flow pages (6 designed + 1 compliance spec), 46 manifest screens, and a Design System page (104 variables, 14 text styles).
- Local workspace: [`frontend/design/figma/`](../frontend/design/figma/README.md) — screen manifest, per-flow notes, **token JSON** in [`tokens/`](../frontend/design/figma/tokens/).
- Runtime tokens: [`frontend/src/constants/tokens.ts`](../frontend/src/constants/tokens.ts) (canonical); `theme.ts` chrome + feature `tokens.ts` re-exports consume it.
- **Color exceptions (2026-09-12 audit):** admin AA primary `#007536`; tote Ocean `#2F80ED`; Live Activity progress amber `#f0ad1e` (≠ status `#fcab29`); camera dim `overlayScrimStrong`. See [brand.md](frontend/brand.md) §Intentional exceptions.
- **Button radius (2026-09-12):** full-width CTAs → `radius.md` (16); inline/dense buttons → `radius.sm` (8); chips + circular FABs → `radius.full`. See [brand.md](frontend/brand.md).
- **Typography (2026-09-12):** Prefer `textStyles` / `AppText` from [`tokens.ts`](../frontend/src/constants/tokens.ts). Bottom nav = `navTab`; status chips = `labelStatus`; standard CTAs = `labelButton`; large onboarding CTAs = `labelButtonLarge`; TopAppBar titles = `headlineTopBar`; in-app live timer = `dataTimer` (Medium 40); Live Activity hero = SemiBold 40 (`dataTimerLiveActivity`). Off-scale sizes collapsed: 13→`bodySmall`, 15→`bodyDefault`/emphasis, 22→`headlineDetail`, 30→`headlinePage`. Documented outliers in [brand.md](frontend/brand.md).
- **Privacy & compliance** documented in [`docs/compliance/`](../docs/compliance/privacy-and-data-protection.md) — COPPA under-13 block with signup PII wipe; universal highest-privacy defaults (opt-in notifications, no teen tier); additional Figma compliance screens designed but not all shipped. In-app Privacy Policy (`/privacy-policy` + four detail routes) uses draft copy effective **July 20, 2026** / last updated **September 1, 2026** from [`privacyPolicyContent.ts`](../frontend/src/features/figma-screens/content/privacyPolicyContent.ts) (MapLibre/CARTO/Esri, Fly.io, Resend — not Google Maps as a processor). As of 2026-08-15 the draft was rewritten against the actual codebase to fix over-claims (then-anonymous auth, Stripe/checkout not live, retention is a target not an enforced job, deletion/export is team-reviewed not instant) and add disclosures that were missing (feedback, service type, session notes, calendar, checkpoint coordinates, session/admin metadata, event registrations, company codes, court-order records, notification content, signature images, Vercel, OSM/Photon/Nominatim/Census/Google Places, court/school/employer PDF recipients); outline in [`mobile-app-privacy-policy-outline.md`](../docs/compliance/mobile-app-privacy-policy-outline.md). Launch board (what still blocks public traffic): [`compliance/launch-checklist.md`](../docs/compliance/launch-checklist.md).
- The Stitch/HTML pipeline is **frozen**. No new screens will be added to `HTML_MAP` or to Stitch.
- Migration is tracked per-screen in [`frontend/design/figma/manifest.yaml`](../frontend/design/figma/manifest.yaml): `designed` → `bound` → `implemented`.

## Sessions + geolocation (implemented — Expo Go test phase)

- **Supabase volunteer auth** — no anonymous sign-in on launch. `AuthProvider` discards leftover `is_anonymous` sessions. Home requires a registered session. Email/password, native Apple (iOS), Google and Facebook in-app OAuth to `nonprofitmobileapp://auth/callback`. Spec: [volunteer-auth.md](frontend/specs/volunteer-auth.md)
- **Fly sessions API** client (`frontend/src/lib/api.ts`, `sessionsApi.ts`) — persists create/checkpoint/finalize when `EXPO_PUBLIC_API_URL` is set. **Finalize is awaited with retry (2026-08-04):** `finalizeLiveSession()` retries the finalize POST up to twice with backoff before giving up (previously fire-and-forget, so a failed sync was silently invisible and the session never reached Supabase); `SubmissionConfirmationScreen` shows a **Retry Sync** banner (`getLastFinalizeSyncFailed`/`retryFinalizeSync`) when it still fails after retries, instead of the blanket "submitted for approval" message
- **GPS + camera** live in Expo Go via `liveSessionStore` (`expo-location` BestForNavigation at **1 s / 0 m**; 2D Kalman position filter with monotonic timestamps and **0.25 s** min predict `dt`; route append gated on **Kalman-resolved accuracy ≤ 25 m**; stationary gate treats missing/`speedMps === 0` as stopped within **2×** min-move of last route point; live pin **frozen at last route point** while stationary; foreground/background dedupe; hardened capture filters — min-move `max(1.4m, accuracy×0.22)`, slow-walk floor **0.14 m/s**, gap recovery, sharp-reversal rejection; mid-session foreground resume preserves Kalman via soft subscription stop; device-compass heading with adaptive EMA + platform accuracy gating and GPS fallback; EMA-smoothed live arrow; live tracker **My Location** control (flyTo + optional follow ~280 ms); **`displayRouteCoordinates` + live tip refreshed every fix** (tip extends only after ≥2 committed route points — no seed→pin fake line while stationary); WebView MapLibre line layer re-ensured each update (`layout` line-join/cap); distance UI shows hundredths under 0.1 mi; lighter live display simplify (~0.55 m + 28-point tail) + tip segment on live maps; **session detail / confirmation replay** uses the same **`simplifyRouteForLiveDisplay`** (not 4 m preview simplify); Douglas-Peucker on legacy 4 m paths where still referenced; live maps wait for first GPS fix with “Getting precise location…” instead of US overview flash). **Live session draft persistence was removed 2026-08-12** — a killed app no longer offers to resume an in-progress session; the volunteer starts fresh next launch (deliberate tradeoff, see `docs/progress.md`). **Background GPS** during active sessions via `expo-task-manager` when Always location is granted (EAS dev build required; **Expo Go is foreground-only** — GPS restarts when the app returns to foreground and re-asserts background task when Always granted; route gaps while locked/backgrounded are expected in Expo Go)
- **WebView map** in Expo Go (MapLibre GL JS + Carto Voyager / Dark Matter) per ADR-005; user pan/zoom on live and preview maps; live tracker map layer picker (Standard / Satellite / Hybrid) wired to `liveSessionStore.mapLayer`; Standard light/dark theme via `mapThemeStore` (auto by local time 7pm–6am, Account toggle to follow time of day, live sun/moon tool to override); **tracker UI chrome** (timer/location/tools/checkpoint/CTAs/Map Types) follows the same theme via `trackerChromeTheme.ts`; dark theme repaints parks/nature reserves/green landcover a legible dark green (upstream Dark Matter renders them invisible against the near-black background — see `mapStyles.ts`); weather pill uses Open-Meteo `weather_code` icons; native MapLibre unchanged for EAS builds
- **Sessions list** refetches from Fly API on focus when `EXPO_PUBLIC_API_URL` is set (pages of 100, not a single 50-row page) — inline spinner + empty CTA (**No sessions logged yet.** / **Log session?** → session setup guide); when API is unset, shows the same empty CTA; **Export record** FAB → multi-select (**Export all** approved-only date range PDF/CSV; **Share** selected via OS sheet; **Cancel**); **Select** → bulk delete non-approved sessions (AsyncStorage tombstones keep deleted ids hidden across restarts). **End session** (`/photo-capture?mode=session-end` Submit) shows Garbagio in the top 75% of the screen (**Saving your session. Meet our mascot, Garbagio, in the meantime!** below); clip plays once then continues to confirmation, and loops only if save is still running.
- **Session detail** fetches from Fly API + Supabase Storage signed URLs via `useSessionDetail`; footer is **Share Feedback**, **Delete session** (when not `approved`), filled tertiary **Go Home** (no stroke); back chevron → Home; **Download PDF** when `approved` (`GET /sessions/:id/service-letter.pdf` via `downloadServiceLetterPdf` + share sheet, and a copy is saved under Account → Letters); after delete, returns to `/sessions-list` with row removed
- **Free-hour paywall** — unpaid live sessions track the orientation hour internally (no visible countdown/pill on the live timer card as of 2026-09-10) and have **no Skip orientation** button; **End Session** (after end photos) logs an **approved 1-hour** free hour, opens session details, and consumes orientation; hour expiry does not open a modal; **pre-track `/tracker-paywall` is hidden** so Track opens session setup directly (route kept for later) — see [free-hour-tracker-paywall.md](frontend/specs/free-hour-tracker-paywall.md)
- **Session photos 18+ (2026-09-01)** — `canUseSessionPhotos()` / live `photosEnabled` disable start, checkpoint, and end camera for known ages 13–17 (missing birthday stays eligible). Under-18 live tracker hides checkpoint UI and alerts; onboarding skips `/camera-permission` then continues free-hour → free-kit. Spec: [minor-session-photos.md](frontend/specs/minor-session-photos.md).
- **Checkpoint grace (reworked 2026-08-12)** — **10 minutes** after a due checkpoint (`CHECKPOINT_MISS_GRACE_MS`); the live tracker card and minimized pill show **"Time elapsed"** (counting up, urgent/red styling) from the instant a checkpoint is due until a photo is submitted — no more forced-end freeze/lock. A missed checkpoint (grace fully expired unsubmitted) is recorded once in `checkpointMisses` on session state for the session's own record, but the window never auto-rolls and tracking never pauses; the volunteer can still submit whenever they take the photo. Session submits as `under_review` when start + end photo pairs exist (mid checkpoints optional). Sound + haptics repeat every 5s (`CHECKPOINT_ALERT_REPEAT_INTERVAL_MS` in `CheckpointAlertLoop`) the entire time a checkpoint is due, and the alert redirects the volunteer to `/live-session` first if they were minimized elsewhere, stacking the `/photo-checkpoint` modal on top so "Back to tracker" lands on the tracker, not wherever they'd been. **This custom sound/haptic loop only runs in the foreground** — it's driven by a plain JS `setInterval`, which iOS suspends once the app backgrounds/locks; nothing calls it from the one real native background task (`backgroundLocationTask.ts`, which only ingests GPS samples). **Locked/backgrounded delivery (2026-09-10):** the scheduled local notification in `checkpointNotifications.ts` now bundles the same alert clip (`photo-checkpoint-alert.wav`, registered via the `expo-notifications` config plugin's `sounds` array in `app.json`) as its custom sound instead of the generic system default — this is the practical ceiling on iOS, which does not allow a custom multi-pulse Taptic pattern to run while locked/backgrounded at all, only a single custom sound + the OS's own vibration on the delivered notification. Requires a native rebuild (prebuild/EAS) to pick up the bundled sound.
- **Checkpoint upload recovery (2026-08-07)** — each `PhotoCheckpointSubmission` tracks a `syncStatus` (`pending`/`synced`/`failed`); photos are always saved to persistent on-device storage before upload (`persistCheckpointPhotos.ts`). The `/live-session` "not yet synced / Retry upload" row was removed 2026-08-12; `retryCheckpointSync` still exists in `liveSessionStore.ts` but has no caller.
- **Backend:** Fastify + Prisma in `backend/sessions/` — deployed to `https://sessions.example.com`; uses Supabase Postgres + JWKS (ES256) auth verification; **service letter PDF** via `@react-pdf/renderer` + OSM static maps (`GET|POST …/service-letter.pdf`); letter assets under `backend/sessions/assets/`
- **GPS trail QA:** Outdoor Expo Go walk confirmed green polyline + distance increments after stationary-gate / WebView layer / distance-format fixes (2026-07-21). EAS + Always lock-screen continuity still open.
- **Checkpoint / finalize sync:** Client **recreates remote session once and retries** on Fly `404 Active session not found` for both checkpoints (`ensureRemoteSession`) and finalize (`createRemoteSessionFromSetup` — works after local teardown). Mid-session sync failures set `sessionSyncWarning`, shown as a banner on `/live-session` (cleared on successful checkpoint sync). Fill `frontend/.env` with **project root** Supabase URL (no `/rest/v1/`) and **anon public JWT** — see [supabase.md](supabase.md). Restart Metro after `.env` changes. Residual 404s usually mean API/env mismatch — see [expo-go-eas-tester-runbook.md](frontend/specs/expo-go-eas-tester-runbook.md).
- **Feedback** — `/session-feedback` (post-session) and `/give-feedback` (Account) share `FeedbackScreen` (rating row **Very Sad → Excited**); Submit → `/feedback-thank-you` (`dismissTo` confirmation or Account). Persisted end-to-end: `POST /feedback` (Fly, `feedbackApi.ts`) inserts into `public.volunteer_feedback`, read back on admin's `/feedback` page.

**API:** `EXPO_PUBLIC_API_URL=https://sessions.example.com` in `frontend/.env`. Fly secrets deployed today: `DATABASE_URL`, `SUPABASE_URL`, `SUPABASE_JWT_SECRET`, `RESEND_API_KEY`, `EMAIL_FROM`, `ADMIN_NOTIFY_EMAIL`. Add `SUPABASE_SERVICE_ROLE_KEY` / `ADMIN_API_KEY` when letterhead/admin API needs them. Local Prisma uses `backend/sessions/.env` (session pooler `DATABASE_URL`). Redeploy Fly after service-letter PDF changes.

**Transactional email (Resend):** Domain verified; admin approve/decline + notify-at-risk (local `.env.local`); event-registration / email-change OTP / admin “session ready for review”. Soft-skips only when the key is unset.

## Not implemented yet

- Supabase dashboard: enable **Email + Apple + Google**, disable Anonymous, add redirect `nonprofitmobileapp://auth/callback` (see [volunteer-auth.md](frontend/specs/volunteer-auth.md)); run `sql/supabase-init.sql` storage policies if bucket missing
- Run `sql/supabase-init.sql` storage policies if the session-photos bucket is missing (verify if not done)
- **Tracker IAP** ($59.99), Vercel Resend env for production admin mail
- **Shop shipping/tracking UX** — mobile checkout supports USPS ship vs office pickup; tracker checkout writes `shop_orders` with optional kit (`includesKit`), shipping FREE. Shop USPS shipping is **25% of paid product-item subtotal**. Admin paid USPS orders: Get rates → Buy label (Shippo) using carton **16×5×2 in @ 1 lb**; **Download label PDF** (primary) and **Go to Shippo** (tertiary). Package `tracking_status` is live from Shippo (Unknown displays as **Pending**).
- Detach unused Fly Managed Postgres cluster (`example-sessions-db`) if provisioned during `fly launch` — app uses Supabase Postgres

## How to run

```bash
npm install --prefix frontend
npm start              # smart: hotspot → LAN; else tunnel (cellular / guest Wi‑Fi)
npm run start:lan      # same Wi‑Fi (fast)
npm run start:hotspot  # Mac on iPhone Personal Hotspot → LAN
npm run start:device   # force tunnel (phone on LTE/5G)
```

Full matrix and troubleshooting: [expo-go-dev-networking.md](frontend/specs/expo-go-dev-networking.md).

**Physical device (Expo Go):**

| Phone connection | Command |
|------------------|---------|
| Same Wi‑Fi as Mac | `npm run start:lan` |
| Mac on iPhone hotspot (`172.20.10.x`) | `npm start` or `npm run start:hotspot` |
| Phone on cellular, Mac on Wi‑Fi | `npm run start:device` |

Metro clears inherited `CI=true` so fast refresh stays enabled. If tunnel/ngrok times out, the start script prints recovery commands.

### EAS development build (background GPS + native MapLibre)

See [accounts-and-access.md](accounts-and-access.md). After installing a development build: `cd frontend && npm run start:dev-client`. **Rebuild required** after `app.json` / `expo-task-manager` plugin changes. Checkpoint photos use **`expo-camera`** in both Expo Go and the dev client.

## Key paths

| Area | Path |
|------|------|
| App routes | `frontend/src/app/` |
| Bundled HTML screens | `frontend/assets/stitch/` *(legacy — frozen)* |
| Figma design workspace | `frontend/design/figma/` |
| Design exports (legacy) | `frontend/design/stitch_htmls/` |
| TS prototype (reference) | `frontend/prototype/` |
