# Expo Go / EAS tester runbook

This sample repository is **not configured** to run in Expo Go or on a device. Backend URLs, EAS project IDs, and store credentials are omitted on purpose.

Read the mobile source under `frontend/` instead.
