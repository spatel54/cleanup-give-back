# Device builds

This sample repository is **not configured** to build, install, or run on a phone. Apple team IDs, EAS project IDs, and store submit config are omitted on purpose.

Read the app source under `frontend/` instead.
