# Figma asset library dump (design-time)

Raw exports from the Figma assets library. **Not bundled by Metro.**

Canonical bundled copies live under `frontend/assets/figma/<screen>/`.
Use `frontend/scripts/organize_screen_assets.py` to sync dump → per-screen folders.

Do not `require()` files from this folder in app code.
