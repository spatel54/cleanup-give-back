export type NavIconProps = {
  /** Fill color — caller passes the active/inactive semantic token color. */
  color?: string;
  /** Rendered width in px. Defaults to the icon's native Figma width. */
  width?: number;
  /** Rendered height in px. Defaults to the icon's native Figma height. */
  height?: number;
};
