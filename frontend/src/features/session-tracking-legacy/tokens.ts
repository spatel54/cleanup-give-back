/**
 * Legacy session-tracking preview — same tokens as the active feature slice.
 */
export {
  primitives,
  colors,
  spacing,
  screenPaddingHorizontal,
  radius,
  shadows,
  fontFamilies,
  textStyles,
  status,
} from '@/features/session-tracking/tokens';

export type { TextStyleName } from '@/features/session-tracking/tokens';
