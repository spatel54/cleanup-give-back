export type IconProps = {
  /** Icon color — defaults to currentColor-equivalent (caller must pass explicitly on RN). */
  color?: string;
  /** Square size in px. Defaults to 24. */
  size?: number;
  /** Stroke width for outline-style glyphs. Defaults to 1.75. */
  strokeWidth?: number;
};
