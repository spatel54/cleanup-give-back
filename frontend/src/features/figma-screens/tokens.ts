/**
 * Re-export of the canonical Figma design tokens.
 * Prefer importing from `@/constants/tokens` for new code.
 */
export {
  colors,
  fontFamilies,
  layout,
  radius,
  shadows,
  textStyles,
  primitives,
  spacing,
  screenPaddingHorizontal,
  status,
} from '@/constants/tokens';

export type { TextStyleName } from '@/constants/tokens';
